-- Task 4: Create first_table with id and name fields
-- This script creates a table called first_table with columns id and name.
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
